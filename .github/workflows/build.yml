name: build

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    name: build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Make scripts executable
        shell: bash
        run: |
          chmod +x termux-build || true
          chmod +x tasctl || true
          chmod +x tools/termux-build-create.sh || true
          chmod +x tools/validate-build.sh || true

      - name: Lint packages (termux-build lint)
        shell: bash
        run: |
          set -e
          for pkg in packages/*; do
            [[ -d "$pkg" ]] || continue
            NAME="$(basename "$pkg")"
            echo "🔍 Linting $NAME"
            ./termux-build lint "$NAME"
          done

      - name: Validate build.sh scripts
        shell: bash
        run: |
          set -e
          for pkg in packages/*; do
            [[ -d "$pkg" ]] || continue
            BUILD="$pkg/build.sh"
            [[ -f "$BUILD" ]] || continue
            echo "✅ Validating $BUILD"
            ./tools/validate-build.sh "$BUILD"
          done

      - name: Generate templates (dry-run)
        shell: bash
        run: |
          ./termux-build template --dry-run

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Run internal build checks
        shell: bash
        run: |
          python ci/run_termux_build.py
